# 中级会计题库 · 使用说明

自建云端题库系统，覆盖 **财管+实务 五年真题（2021-2025）+ 三年模拟（2026）共 632 题**，并已合并 **刘阳《基础速成》精选例题（实务 25 章，283 题）** 与 **斯尔《专属练习》题库（实务 25 章 + 财管 10 章，1160 题）**，总计 **2075 题**。

- **网站地址**：https://lmrean.github.io/IAT-exam/
- **数据存储**：私有仓库 `Lmrean/sier-data`（做题记录 / 错题 / 收藏，自动同步到 GitHub）
- **离线可用**：PWA，可添加到手机主屏幕，无网也能刷题（同步需联网）

---

## 一、首次使用（重要）：生成访问令牌

网站通过 GitHub 令牌读写你的私有数据仓库。仅需一次，令牌只保存在本设备浏览器。

1. 电脑浏览器打开 GitHub，登录账号 `Lmrean`（已创建好仓库的那位）。
2. 右上角头像 → **Settings** → 左侧 **Developer settings** → **Personal access tokens** → **Fine-grained tokens**。
3. 点 **Generate new token**。
4. 填写：
   - **Token name**：随意，如 `IAT-exam`
   - **Expiration**：建议 90 天（到期后重新生成并更换即可）
   - **Repository access**：选 **Only select repositories** → 勾选 **`sier-data`**
   - **Permissions** → 展开 **Repository permissions** → 找到 **Contents** → 选 **Read and write**
5. 拉到最底点 **Generate token**，复制生成的 `github_pat_` 开头的令牌（**只显示这一次，务必立即复制**）。
6. 打开网站 https://lmrean.github.io/IAT-exam/ ，输入：
   - **令牌**：粘贴刚复制的 `github_pat_...`
   - **用户名**：自定义一个标识（如 `zhangsan`），用于区分多台设备的数据
7. 点 **登录 / 连接**。看到提示"已同步"即成功。

> 换设备（iPhone / 另一台电脑）时：重复第 6 步，输入**同一个令牌和同一个用户名**，即可同步全部做题记录和错题。

---

## 二、功能介绍

| 功能 | 位置 | 说明 |
|---|---|---|
| 选择题库 | 刷题页顶部 | **真题模拟**（632 题）/ **基础速成**（刘阳例题 283 题）/ **专属练习**（斯尔 1160 题） |
| 刷题 | 刷题 → 按卷练习 | 真题：选科目 → 选年份卷 → 可选题型范围 → 开始 |
| 章节练习 | 刷题 → 基础速成 / 专属练习 | 章节**下拉多选 + 全选** + 题型**筛选** → 按所选练习（两库共用同一套章节/题型选择器） |
| 随机练习 | 刷题 → 随机 20 题 | 当前题库/筛选范围随机抽 20 题 |
| 模拟考 | 刷题 → 模拟考 | 自定题量 + 限时倒计时，交卷出得分率 |
| 错题本 | 错题本 | 自动收集答错题；可一键重练 / 打印导出 |
| 统计 | 统计 | 已做总量、正确率、分科目正确率、最近活动 |
| 搜题 | 搜题 | 按关键词搜索（题干/答案/解析，含表格内容） |
| 收藏 | 做题时 ☆ 收藏 | 快速收藏重点题（数据存云端） |
| 设置 | 设置 | 每日目标、立即同步、清除本地数据 |

**基础速成（章节练习）用法**：刷题页顶部切到"基础速成" → 默认全选 25 章 → 点章节/题型取消不想要的 → 选练习方式：
- **按所选练习**：按当前筛选顺序逐题刷（含客观题 + 主观题）
- **随机 20 题**：当前筛选范围内抽 20 题
- **模拟考**：自定题量与限时，交卷出得分率

**专属练习（章节练习）用法**：刷题页顶部切到"专属练习·实务+财管"（1160 题，含实务 25 章 + 财管 10 章）→ 顶部先选**科目**（实务专属练习 / 财管专属练习，两科章节独立）→ 章节**下拉多选 + 全选**（实务 25 章、财管 10 章）→ 题型筛选 → 选练习方式（按所选 / 随机 20 / 模拟考）。主观题（计算分析、综合题）含表格解析与分步答案，作答方式同下。

**真题模拟（按卷练习）用法**：刷题页顶部切到"真题模拟" → 选**科目** → 勾选**试卷**（可多选：如 2021 真题 + 模拟一 + 模拟二）→ 勾选**题型**范围（可多选）→ 开始。试卷 / 题型均为多选，与基础速成、专属练习统一为同一套筛选交互。

**主观题（计算分析/综合/变形）作答方式**：先看题在纸上作答，点"交卷 / 判分"后对照参考答案（含表格解析），点 **✓ 答对了** 或 **✗ 答错了** 自评，结果计入正确率与错题本。

---

## 三、添加到手机主屏幕（推荐）

**iPhone（Safari）**：打开网站 → 底部分享按钮 → **添加到主屏幕** → 确认。之后从主屏幕图标打开即全屏 App 体验，离线可刷题。

**安卓（Chrome）**：打开网站 → 右上角菜单 → **添加到主屏幕** / **安装应用**。

---

## 四、打印 / 导出错题

1. 打开 **错题本** → 点 **打印 / 导出错题**。
2. 新窗口显示全部错题（题干 + 答案 + 解析）。
3. 浏览器菜单 → 打印，可选"另存为 PDF"导出到电脑，方便纸质复习。

---

## 五、常见问题

**Q：网站能打开但登录失败 / 提示"令牌无效"？**
- 令牌权限没选对：确认 Repository access 只勾选了 `sier-data`，且 Contents 权限为 **Read and write**。
- 令牌过期：90 天有效期到期后需重新生成。
- 换设备后令牌只在当前设备生效，需在新设备重新输入。

**Q：换了电脑，做题记录怎么找回？**
- 用同一 GitHub 账号 + 同一用户名登录即可，记录、错题、收藏会自动从云端拉取。

**Q：数据会不会泄露？**
- 题库数据（2075 题）是公开的（供离线加载）；你的做题记录存在**私有仓库** `sier-data`，只有你的令牌能读写，GitHub 上其他人不可见。

---

## 六、维护者信息（技术）

- 前端仓库：`Lmrean/IAT-exam`（公开，GitHub Pages 自动部署）
- 数据仓库：`Lmrean/sier-data`（私有）
- 题库来源：
  - 斯尔《5年真题+3年模拟》财管+实务 4 个 PDF，已解析为结构化 JSON（632 题，ID 1–632）
  - 刘阳《基础速成·精选例题讲解》Word 讲义（实务 25 章，283 题，ID 10001–10283；主观题含表格解析 `analysis_html`）
  - 斯尔《专属练习》Word 讲义（实务 25 章 + 财管 10 章，1160 题，ID 20001–21160；计算/综合题含表格分步解析；源文件 5 题缺答案，已标 `no_answer`）
- 技术栈：纯静态 PWA（HTML/CSS/JS + Service Worker），无后端，用 GitHub Contents API 同步数据
- 本地源码目录：`G:\Desktop\讲义\斯尔-五年真题三年模拟\sier-app\`
- 解析脚本：
  - `G:\WorkBuddy\2026-08-18-10-03-05\parse_sucheng.py`（基础速成 Word → JSON，283 题）
  - `G:\WorkBuddy\2026-08-18-10-03-05\parse_zhuanshu.py`（专属练习 题目本+答案本 Word → JSON，1160 题）

---

## 七、更新部署（推送到 GitHub Pages）

> 本地 `sier-app\` 是普通目录（非 git 仓库）。推荐用 **clone → 覆盖 → 推送** 的方式上线，保留线上历史且不会误伤其他文件。

**1. 克隆线上仓库到临时目录并覆盖**

```bash
cd /d G:\Desktop
git clone https://github.com/Lmrean/IAT-exam.git IAT-exam-deploy
:: 将本地最新源码整体覆盖进去（含 data/bank.json、app.js、style.css、sw.js、index.html）
xcopy "G:\Desktop\讲义\斯尔-五年真题三年模拟\sier-app" "G:\Desktop\IAT-exam-deploy" /E /Y
```

**2. 提交并推送**

```bash
cd /d G:\Desktop\IAT-exam-deploy
git add .
git commit -m "合并专属练习题库：新增1160题(实务25章+财管10章)，三库统一筛选交互(total 2075)"
git push origin main
```

GitHub Pages 检测到 `main` 分支更新后会自动重新部署（约 1 分钟），之后访问 https://lmrean.github.io/IAT-exam/ 即为新版。

**3. 无需动 `sier-data` 私有仓库**（做题记录/错题/收藏的同步逻辑未变，浏览器会自动读写）。

> 注意：
> - 若 `G:\Desktop\IAT-exam-deploy` 已存在（之前克隆过），先删掉再 clone，避免残留文件。
> - 若推送提示需要身份：`git config user.name "Lmrean"`、`git config user.email "你的邮箱"`。
> - 用户手机端旧缓存：`sw.js` 中 `CACHE` 已升级为 `sier-v4`，打开网站即自动拉新。
